#!/usr/local/projects/routing/help/html_filter.pl
<h2 align=center>Help for the "Distributor" field of the "Route" table</h2>
This field contains the Distributor associated with the route.  A route can only be long to one
distributor, so in practice, it will never change.

